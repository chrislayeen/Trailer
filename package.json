{
  "name": "trailer-assembly-portal",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "admin-portal"
  ],
  "scripts": {
    "frontend:dev": "npm run dev --workspace=frontend",
    "frontend:build": "npm run build --workspace=frontend",
    "admin:dev": "npm run dev --workspace=admin-portal",
    "admin:build": "npm run build --workspace=admin-portal",
    "build": "npm run build --workspace=frontend"
  }
}